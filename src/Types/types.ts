import { ReactNode } from 'react';

export type AnchorArray = {
  ids: string[];
};

export type Multiply = {
  multiply: number;
};

export type Offset = {
  offsetY: number;
};

export type Children = {
  children: ReactNode;
};
