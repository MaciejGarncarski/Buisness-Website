import styled from 'styled-components';

const Message = styled.h2`
  text-align: center;
  font-weight: 400;
  font-size: 1em;
  position: absolute;
  bottom: 1em;
  left: 0;
  width: 100%;
`;

export default Message;
